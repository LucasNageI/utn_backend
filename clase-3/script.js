"use strict";
let number;
if (number) {
    console.log(number);
}
else {
    console.log("no hay numeros aun");
}
let nombre = "lucas";
let is_registered = true;
const calcularIva = (precio) => {
    return Number(precio) * 0.21;
};
const mostrarDatoCualquiera = (dato) => {
    return dato;
};
